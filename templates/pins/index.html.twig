<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>Ping</title>
    </head>
    <body>
        <a href="{{ path('app_pins_create') }}" >Create Pin</a>
        {% for pin in pins %}

            <a href="{{path('app_pins_shwo', {'id': pin.id})}}">
                <article>
                    <h1>{{ pin.title }} </h1>
                    <p> {{ pin.description  }} </p>
                </article>
            </a>
            {% else %}
                <p>Sorry, no pin !</p>
        {% endfor %}
    </body>
</html>