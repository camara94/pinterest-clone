<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>Ping</title>
    </head>
    <body>
        {% for pin in pins %}

            <article>
                <h1>{{ pin.title }} </h1>
                <p> {{ pin.description  }} </p>
            </article>
        {% endfor %}
    </body>
</html>